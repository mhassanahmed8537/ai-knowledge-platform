apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api
spec:
  tls:
    - hosts: ["dev.knowledge-platform.example.com"]
      secretName: api-tls
  rules:
    - host: dev.knowledge-platform.example.com
